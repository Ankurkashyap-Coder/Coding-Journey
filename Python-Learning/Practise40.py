# Power of N

user_base = int(input("Enter the Base "))
user_power_of_base = int(input("Enter the power applied on the Base "))

result = user_base ** user_power_of_base

print("The power of Base is :", result)